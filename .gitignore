# Build output
bin/
obj/
publish/
dist/
build/
*.deb

# Editor / IDE
.vs/
*.user
.idea/

# Local agent / editor session settings
.claude/

# Local .NET install (if placed in-tree)
.dotnet/

# --- secrets / credentials: never commit these ---
git.txt
*token*
*secret*
*.pem
*.key
id_rsa*
*-private.*
*.gpg
!packaging/**         # (scripts here are safe to track)
.gnupg/
.env
